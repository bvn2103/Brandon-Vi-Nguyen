# Profile

Long-form personal profile materials live here.

- `BIO.md` — symlink to the canonical root biography
- `RESUME.md` — symlink to the canonical root resume

The repository root remains the source of truth for the canonical portfolio-checklist files `BIO.md` and `RESUME.md`.
The entries in this directory are symbolic links so the documented `profile/` structure still exists after the reorganization without introducing a second manually maintained copy.
